// Player dies, draft//

if(Hp => 0) {

}
While (isdirty == false ) 
{console.log () 
    
 process.exit}
 
// restart

let restart = await rl.question (dictionary.en.playagain);
if ( playagain == R ){
    console.log ()
}

// new item/ 
    { name: DICTIONARY.en.pickup3, attribute: DICTIONARY.en.hpGain, value: (2-4);
        Math.round(Math.random() * (4 - 2)) + 2 }
    { name: DICTIONARY.en.pickup4, attribute: DICTIONARY.en.attackDmg, value: (1-3);
         Math.round(Math.random() * (3 - 1)) + 1 }

// how to do it, what is wrong. 

async function eventTime(){
    return new promise((eventText)=> setTimeout(eventText, 2000));
}
await eventTime();

/*Array.prototype.random = function () {

    let index = Math.round(Math.random() * this.length)
    return [index]
}
*/
